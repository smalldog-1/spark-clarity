# Contributing to Spark Clarity

感谢你对 Spark Clarity 的兴趣！我们欢迎各种形式的贡献。

## 如何贡献

### 报告问题

如果你在使用过程中发现问题或有改进建议：

1. 检查 [Issues](../../issues) 确保问题未被报告
2. 创建新 Issue，清楚描述：
   - 问题现象或改进建议
   - 期望的行为
   - 如果是 bug，提供复现步骤

### 改进文档

文档改进永远欢迎：
- 修正错别字
- 改进表述清晰度
- 添加使用示例
- 翻译成其他语言

### 改进核心 Skill

如果你想改进 `SKILL.md` 本身：

1. Fork 仓库
2. 创建特性分支 (`git checkout -b feature/improvement`)
3. 测试你的修改（实际使用并验证效果）
4. 提交变更 (`git commit -m 'Add some improvement'`)
5. 推送到分支 (`git push origin feature/improvement`)
6. 创建 Pull Request

**测试建议**：
- 每次改动后运行 [TESTS.md](TESTS.md) 的固定用例（触发开场 / 全流程 / 边界情况）
- 用真实想法测试完整流程
- 验证最终文档有判断、而非对对话的复述

### 添加变体

如果你想创建针对特定领域的变体（如针对学术研究、商业分析等）：

1. 在 `variants/` 目录下创建新文件
2. 说明该变体的适用场景和差异
3. 提供示例

## 设计原则

改进时请遵循这些核心原则：

1. **分工铁律**：主观内容（缘起、特色、概念）只来自用户，重述而不编造；调研（Scout）与文档判断（Distill）由 AI 独立完成——绝不把调研变成访谈
2. **固定问话**：Spark 环节的两句固定问话逐字使用（见 SKILL.md），环节内不追问、不评论、不改写；改动措辞须先讨论
3. **一问一答**：一次只问一个问题；两句固定问话是设计好的复合句，豁免于此规则，不允许拆分
4. **最小追问**：Frame 只在"还不知道查什么"时才问，不逼用户回答调研能带回来的问题（如"面向谁"）
5. **调研优先联网**：有 web 工具必须使用；没有则基于模型知识并注明截止时间；绝不编造产品、数据或引用
6. **判断而非复述**：文档每一节都要有前文未出现的结论；定位最后写、必须给立场并引用证据
7. **来源可追溯**：文档内标注「来自用户 / AI-research / AI 综合判断」，边界不混
8. **通用性**：适用于多种类型的想法，不局限于特定领域

## 行为准则

- 尊重和建设性的交流
- 欢迎新手和各种背景的贡献者
- 专注于改进产品而非个人

## 问题？

有任何疑问欢迎在 Issues 中提出！

---

再次感谢你的贡献！🙏
